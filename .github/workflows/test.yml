name: Test Runner User

on:
  push:
    branches:
      - main # or any branch you want to trigger on

jobs:
  check-user:
    runs-on: self-hosted # ensures it runs on your self-hosted runner
    steps:
      - name: Print username
        run: whoami

      - name: Print detailed user info
        run: id
